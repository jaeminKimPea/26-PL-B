# 5주차 1day Report
## GCC 컴파일
- 목적 파일 만들기
> gcc -c hello.c → hello.o 
- 원하는 이름으로실행 파일 만들기
> gcc -o hello hello.c → hello
> ./hello
- 디버그 가능하고원하는 이름으로실행 파일 만들기
> gcc –g –o hello hello.c → h

## GDB 디버깅
- gdb hello
> gcc –g –o hello hello.c
- q 나가기
## make 종속성 구성도구
-예)
>say-hello: main.o getname.o
>    gcc –o say-hello main.o getname.o
>main.o: main.c
>    gcc –c main.c
>getname.o : getname.c
>    gcc –c getname.c

## C File I/O
- 변수 형태
>FILE *infile , *outfile ;
- 파일 열기
> r , w, a 모드
>infile = fopen (“file_name ”, “r”); outfile = fopen (“file_name ”, “w”);
- 파일에 입력
>r – fscanf (infile , “%d”,&i); fscanf (infile , “lf”, &d); fscanf (infile , “%s”,
string)
> 문자열은 자체가 주소다
- 파일에 출력
>w – fprintf (outfile , “val = %d\ n”, i);
- 닫기
>fclose (infile ); fclose (outfile )